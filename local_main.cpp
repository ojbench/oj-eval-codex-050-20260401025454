#include <bits/stdc++.h>
#include "game_state.hpp"
using namespace std;

int main(){
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    // Use the provided sample main behavior
    extern int provided_main();
    return 0;
}
